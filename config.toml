# The URL the site will be built for
base_url = "https://dustinknopoff.dev"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

feed_filename = "rss.xml"
generate_feed = true

taxonomies = [{name = "tags", rss = true}, {name = "categories", rss = true}]

description = "rustacean, cook, and martial arts enthusiast"
title = "Dustin Knopoff"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
render_emoji = true
# Whether external links are to be opened in a new tab
# If this is true, a `rel="noopener"` will always automatically be added for security reasons
external_links_target_blank = true
# Whether to set rel="nofollow" for all external links
external_links_no_follow = true
# Whether to set rel="noreferrer" for all external links
external_links_no_referrer = true
extra_syntaxes = ["syntaxes", "syntaxes/pest"]

[search]
# Whether to include the title of the page/section in the index
include_title = true
# Whether to include the description of the page/section in the index
include_description = false
# Whether to include the rendered content of the page/section in the index
include_content = true

[extra]
# Put all your custom variables here
forage_author = "Dustin Knopoff"
forage_email = "javascript:window.location.href=atob('bWFpbHRvOndlYi1jb250YWN0QGtub3BvZmYuZGV2')"
forage_github = "https://github.com/dustinknopoff"
forage_linkedin = "https://linkedin.com/in/dustinknopoff"
forage_microblog = "dustinknopoff"
forage_sidebar = [
  {label = "dev", url = "$BASE_URL/categories/dev"},
  {label = "design", url = "$BASE_URL/categories/design"},
  {label = "tags", url = "$BASE_URL/tags"},
]
